import { MethodPage } from "@/components/method/method-page";

export const metadata = {
  title: "Method — Bureau of Classification",
  description:
    "The full FSC classification pipeline: every prompt, every rule, and the design tradeoffs behind them.",
};

export default function Method() {
  return <MethodPage />;
}
